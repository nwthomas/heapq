{
  "name": "@nwthomas/heapq",
  "module": "src/index.ts",
  "types": "./lib/index.d.ts",
  "main": "./lib/index.js",
  "files": [
    "lib/**/*"
  ],
  "scripts": {
    "clean": "del-cli ./lib",
    "build": "bun run clean && tsc -p ./tsconfig.json",
    "prepack": "bun run build",
    "test": "jest ./test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "type": "module"
}
